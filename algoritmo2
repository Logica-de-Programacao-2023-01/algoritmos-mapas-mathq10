package main

func mapao(mapa1, mapa2 map[string]int) map[string]int {
	mapasso := make(map[string]int)

	for key, value := range mapa1 {
		mapasso[key] = value
	}
	for key, value := range mapa2 {
		mapasso[key] = value

	}
	return mapasso
}
